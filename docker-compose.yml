version: "3.7"
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile-deploy
    container_name: gpt-front
    env_file:
      - .env
    expose:
      - 3000
    networks:
      - proxy
networks:
  proxy:
    external: true